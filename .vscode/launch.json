{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "debug daemon w config",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/daemon",
            "cwd": "${workspaceFolder}",
            "env": {},
            "args": [
                "-c",
                "config/config.toml"
            ]
        }
    ]
}